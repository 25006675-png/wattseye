# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Coach runtime state
ML/insights/coach/_weather_cache/
ML/insights/coach/_whatsapp_sent.json
ML/insights/coach/_user_actions.json

# Pi runtime state — written by pi_bridge.py, read by api_server.py
backend/live_state.json
# User-set AC smart rule + appliance labels — runtime state, not source
backend/_smart_rule.json
backend/_appliance_labels.json

# OS / editor / agent
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

.agents/

backend/reminders.json

# Downloaded third-party datasets (large; not part of the repo)
datasets/
flutter-web.err
wattseye_app/flutter-web.err
flutter_run.log

# Local demo/runtime helpers with secrets or one-off test output
set_token.sh
test_rule.sh
test_empty_card.sh
run_remind_test.sh
